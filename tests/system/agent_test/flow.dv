# Test flow for Agent task

package:
  name: agent_test

  tasks:
    - name: simple_agent
      uses: std.Agent
      with:
        user_prompt: "Generate a simple hello world message"
        assistant: "copilot"
